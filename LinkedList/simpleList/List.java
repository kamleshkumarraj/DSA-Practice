package LinkedList.simpleList;

public class List {
    Node head;
    Node tail;

    public static void main(String[] args) {
        Node a = new Node(10);
        Node b = new Node(20);
        Node c = new Node(30);
        Node d = new Node(40);
        Node e = new Node(50);
    }
}
